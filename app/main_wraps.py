# coding:utf8
# a1=db.session.query(role_auth_table).filter_by(role_id==1).all()
# auths=[v.auth_id for v in a1]
# a=Auth.query.filter_by(url=str(requset.url_rule))
# if a not in auths:
#     abort(404)
# return f(*args,**kwargs)
